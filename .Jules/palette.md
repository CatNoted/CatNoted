## 2026-07-25 - Canvas Properties A11y
**Learning:** Icon-only controls in property panels like CanvasProperties often lack ARIA labels and titles, hindering accessibility.
**Action:** Add `aria-label` and `title` to all such buttons (e.g., border, alignment, nudge) and `aria-label` to inputs like sliders.
