🚀 **Audit Fixes**

This PR contains fixes after performing a codebase audit across all workspaces.

**Fixes:**
1. **Typescript/Build (Export mismatch/circular import avoidance)**
   - Fixed `packages/editor/tsconfig.json` by removing hardcoded `paths` for cross-package imports in favor of standard workspace resolution.
2. **UI (Tokens/Colors)**
   - Fixed hardcoded Tailwind colors (`indigo`) to semantic token (`accent`) in `SlashCommandMenu` and `ForceGraph`.

**What was not fixed & Why:**
- There were no `typecheck`, `test` or `build` errors found in the initial run.
- Tests (e2e, vitest) all pass successfully, no broken assertions or stale mocks were identified.
- There are no circular dependencies (verified with `dpdm`).
- No missing or orphaned imports found that break the build.
- `AGENTS.md` and `README.md` are well-structured and there's no need to update them for these internal config/UI changes.
- Database schemas/types (in `crypto.ts` and `AppLayout.tsx`) appear correct and functional.

**Verification:**
- [x] `pnpm typecheck` passed
- [x] `pnpm test` passed
- [x] `pnpm build` passed
