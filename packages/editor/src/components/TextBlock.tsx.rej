--- HeadingBlock.tsx
+++ HeadingBlock.tsx
@@ -85,7 +85,7 @@

       if (e.key === ' ' || e.key === 'Enter') {
         // Check for heading markdown like "# ", "## " etc.
-        const match = localContent.match(/^(#{1,3})\s+(.*)$/);
+        const match = localContent.match(/^(#{1,3})\s+/);
         if (match) {
           // We keep the # so it's a heading
         }
@@ -94,7 +94,7 @@
         // Check if they deleted the #, maybe it should become a text block
         if (localContent === '') {
           onConvert('text');
-        } else if (!localContent.match(/^(#{1,3})/)) {
+        } else if (!localContent.match(/^(#{1,3})\s+/)) {
           // Wait, usually heading inputs enforce starting with #
         }
       }
--- TextBlock.tsx
+++ TextBlock.tsx
@@ -84,7 +84,7 @@
         onNewline();
       } else if (e.key === ' ') {
         // Check if they are trying to convert to a heading like "# "
-        const match = localContent.match(/^(#{1,3})$/);
+        const match = localContent.match(/^(#{1,3})\s*/);
         if (match && localContent.trim().length <= 3) {
           // Actually we'd need to emit a transform event, but let's keep it simple
           // In a real editor this would convert the block
@@ -93,7 +93,7 @@
       } else if (e.key === 'Backspace' && localContent === '') {
         e.preventDefault();
         onRemove();
-      } else if (e.key === 'Backspace') {
+      }
     }
   };
